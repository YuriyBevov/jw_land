mixin lw-main-slider(data)
  .lw-main-slider
    .swiper
      .swiper-wrapper
        each slide in data.slides
          .swiper-slide
            img(src=`../assets/img/${slide.img}` alt="" width="550" height="550")

      .swiper-button-prev
      .swiper-button-next
